enum LogLevel {
  debug,
  info,
  warn,
  error,
}

enum LogFilter {
  all,
  debug,
  info,
  warn,
  error,
  none,
}

enum EntityType {
  viewModel,
  screen,
  service,
  repository,
  model,
  provider,
  unknown,
}
