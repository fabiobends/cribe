#!/bin/sh

echo "🔍 Running pre-commit hook..."

# Get all staged Dart files
STAGED_FILES=$(git diff --cached --name-only --diff-filter=d | grep '\.dart$')

if [ -z "$STAGED_FILES" ]; then
    echo "✅ No Dart files to check."
    echo "🎉 Pre-commit hook completed successfully!"
    exit 0
fi

COUNT=$(printf "%s\n" "$STAGED_FILES" | wc -l | tr -d ' ')
echo "📝 Found ${COUNT} Dart file(s) to process..."

# Run format-fix script which handles formatting, fixes, and analysis
scripts/format-and-fix
if [ $? -ne 0 ]; then
    echo "❌ Format and fix failed. Please fix the issues before committing."
    exit 1
fi

# Re-stage only the originally staged Dart files (handles spaces/newlines safely)
echo "📦 Re-staging formatted and fixed files..."
printf "%s\n" "$STAGED_FILES" | while IFS= read -r file; do
    [ -n "$file" ] || continue
    git add -- "$file"
done

echo "✅ All checks passed!"
echo "🎉 Pre-commit hook completed successfully!"